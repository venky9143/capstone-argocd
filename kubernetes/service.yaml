apiVersion: v1
kind: Service
metadata:
  name: capstone-service
spec:
  type: NodePort
  selector:
    app: capstone
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
      protocol: "TCP"
      